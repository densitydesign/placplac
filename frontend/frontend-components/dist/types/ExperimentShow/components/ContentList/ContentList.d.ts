import { LanguageOptions } from "../../..";
import { Experiment } from "../../../types";
interface ContentListProps {
    experiment: Experiment;
    language: LanguageOptions;
}
export declare const ContentList: (props: ContentListProps) => JSX.Element;
export {};
